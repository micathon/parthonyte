do (
gdefun (var x y w z a) do (
  set x 0L;
  ++ x;
  set y 10L;
  -- y;
  println "x, y:" x y;
  f;
);
defun (f) (var x y) do (
  set x 0L;
  ++ x;
  set y 10L;
  -- y;
  println "x, y:" x y;
  set x 1.0;
  #++ (+ x 0);
);
)

