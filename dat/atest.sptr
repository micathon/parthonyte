do (
gdefun (var a b c) do (
  set a -500;
  println a;
  f a (g);
  set c (inc 100);
  set a (- 60 2);
  set b 747;
  println a (/ a 2) b c;
  println b (f 0 61);
  g;
  f (+ 2 a (* c 2))(g);
  hello;
  println a;
);
defun (f n m)(var x y) do (
  println n;
  set x 77;
  set y 99;
  set n 255;
  println x y m;
  g;
  return 333;
); 
defun (g)(var m) do (
  set m 800;
  println m;
  return m;
);
defun (inc x) do (
  return (+ x 1);
);
defun (hello) do (
  println -9999;
);
)

