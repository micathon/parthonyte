do (
gdefun (var a b) do (
  #set a (+ 6 (/ 8 0));
  f 23;
  println "btm";
);
defun (f x) (var i) do (
  println "f, x:" x;
  set x (+ x 0);
  if (ne x 23) do (
    println "if";
  )
  elif (eq x 23) do (
    println "elif";
    #set x (/ x 0);
  )
  else do (
    println "else";
  );
  set i 0;
  while (< i 3) do (
    += i 1;
    println i;
    #set x (/ x 0);
  );
  println "f: btm";
);
)

