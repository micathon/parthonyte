do (
? -2.0;
? 100.6;
defun (p)
  do (
  for i in (add x);
);
)
@
do (
? 100.7;
defun (p)
  do (
  for i in (add x y);
);
)
@
do (
defun (p)
  do (
  for (= i 1; ? (<= i n); ++ i) do (
    += sum i;
  );
);
)
@

