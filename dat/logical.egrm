do (
gdefun do (
  andop;
  #questop;
);
defun (andop) (var a x c d) do (
  set c true;
  set d false;
  println "cd:" c d;
  set x (&& (f c 1) (f d 2));
  println "x:" x;
);
defun (f flag n) do (
  println "n:" n;
  return flag;
);
defun (questop) (var c d flag x a b) do (
  set flag false;
  set a 101;
  set b 202;
  set x (quest flag (fun a) (fun b));
  println "x:" x;
);
defun (fun x) do (
  println "y:" (+ x 1);
  return x;
);
)

