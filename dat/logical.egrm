do (
gdefun do (
  andop;
  #questop;
);
defun (andop) (var a x c d e) do (
  set a false;
  set c true;
  set d false;
  set e false;
  println "cde:" c d e;
  set x (&& c d e);
  println "x:" x;
);
defun (questop) (var c d flag x a b) do (
  set flag false;
  set a 101;
  set b 202;
  set x (quest flag (fun a) (fun b));
  println "x:" x;
);
defun (fun x) do (
  println "y:" (+ x 1);
  return x;
);
)

